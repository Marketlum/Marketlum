services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: marketlum
      POSTGRES_PASSWORD: marketlum
      POSTGRES_DB: marketlum
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
