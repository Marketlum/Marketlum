import type { MarketlumWebPlugin } from '@marketlum/ui';

/**
 * Active web plugins for this app — the frontend mirror of api/src/plugins.ts.
 * Add `@marketlum/plugin-*/web` entries here; imported only by client code.
 */
export const webPlugins: MarketlumWebPlugin[] = [];
